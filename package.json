{
  "name": "slack-remote-functions-on-cloudflare",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240423.0",
    "bun-types": "^1.1.4",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vitest": "^1.5.2",
    "wrangler": "^3.52.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --port 3000",
    "deploy": "wrangler deploy",
    "format": "npx prettier -w src",
    "test": "vitest"
  },
  "dependencies": {
    "slack-cloudflare-workers": "^0.12.0"
  }
}
